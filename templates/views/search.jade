extends ../layouts/default

block content

	section#search
		header
			h2 RealGen Search
			p Enter your MLS(Multiple Listing Service) Number to Begin:
		
		form(method='post')
			input(type='text' name="MLS" id='MLSField')
			button(type='submit' id='MLSsubmitButton') Send
	
		footer
			if user && user.canAccessKeystone
				p
					a(href='/keystone', style='margin:10px 10px 0px 0px').btn.btn-lg.btn-primary Open the Admin UI
			else
				p We have created a default Admin user for you with the email <strong>joshuasun@leizhang.com</strong> and the password <strong>Sj82621338</strong>.
				p
					a(href='/keystone/signin', style='margin-right:10px').btn.btn-lg.btn-primary Sign in

block intro
	if pass == false
		.error MLS Number is Invalid